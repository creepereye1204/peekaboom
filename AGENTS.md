# AGENTS.md — PEEKABOOM

에이전트가 이 저장소에서 작업할 때 **가장 먼저 읽는 파일**. 여기 적힌 규칙은 다른 모든 문서보다 우선한다.

---

## 1. 이 프로젝트가 뭔지

**PEEKABOOM** — 브라우저에서 돌아가는 서버 없는 멀티플레이 숨바꼭질 게임.

- 모드 2개: **숨바꼭질(Hide & Seek)**, **럼블(Rumble)**
- 렌더링: Rust → WASM, WebGL2 직접 제어
- 네트워크: WebRTC DataChannel P2P (Trystero 시그널링), **호스트 권위(host-authoritative)**
- 배포: GitHub Pages 정적 호스팅. **런타임 서버 비용 0원**
- 대상: 데스크톱 + 모바일 브라우저 (터치 조작 1급 시민)

한 줄 요약: *"링크 하나 보내면 6명이 30초 안에 들어와서 숨바꼭질하는 게임."*

---

## 2. 절대 깨면 안 되는 제약 (Hard Constraints)

이걸 어기는 PR은 리뷰 없이 반려된다.

| # | 제약 | 이유 |
|---|---|---|
| H1 | 런타임에 우리가 운영하는 서버가 **하나도** 없어야 한다 | 서버비 0원이 프로젝트 존재 이유 |
| H2 | 빌드 산출물은 `docs/` 또는 `gh-pages` 브랜치의 정적 파일뿐 | GitHub Pages 배포 |
| H3 | **모든 게임 상태 변경은 호스트 권위**를 거친다. 피어가 직접 상태를 바꾸지 않는다 | 치트 방지 (SECURITY.md §2) |
| H4 | 술래가 아닌 피어에게 **숨은 사람의 좌표를 보내지 않는다** | 메모리 핵 / 패킷 관찰 월핵 차단 |
| H5 | WASM 번들(gzip) ≤ **1.5 MB**, 최초 인터랙션까지 ≤ **3초** (4G, 중급 안드로이드) | 모바일 이탈률 |
| H6 | 모든 입력은 `InputIntent`로 추상화. 게임 로직은 키보드/터치/게임패드를 구분하지 않는다 | FRONTEND.md §3 |
| H7 | 에셋은 **CC0 또는 그에 준하는 것만**. 추가 시 `docs/generated/asset-manifest.md`에 출처·라이선스 기록 필수 | 법적 리스크 0 |
| H8 | `unsafe` Rust 금지. 예외는 design-doc에 근거를 적고 `// SAFETY:` 주석 필수 | 코드 품질 |

---

## 3. 문서 우선 (Harness Engineering)

**코드보다 문서가 먼저다.** 이 저장소는 "문서가 사양이고 코드는 그 구현"이라는 전제로 굴러간다.

작업 순서:

```
1. docs/product-specs/  에서 "뭘 만드는지" 확인
2. docs/design-docs/    에서 "왜 이렇게 만드는지" 확인
3. docs/exec-plans/active/ 에서 "지금 어느 단계인지" 확인
4. 코드 작성
5. 문서와 코드가 어긋났으면 → 문서를 고치는 PR을 먼저 낸다
```

### 새 기능을 추가할 때

- **제품 동작이 바뀐다** → `docs/product-specs/`에 스펙 먼저
- **기술 선택을 한다** → `docs/design-docs/`에 결정 기록(ADR 형식) 먼저
- **여러 세션에 걸친 작업이다** → `docs/exec-plans/active/NNN-<slug>.md` 먼저

### 문서를 고칠 때

- `docs/generated/` 아래는 **손으로 고치지 않는다**. 생성 스크립트를 고친다.
- `docs/references/` 아래는 외부 라이브러리 요약 캐시다. 우리 결정을 여기 적지 않는다.
- 인덱스(`index.md`)가 있는 디렉터리에 파일을 추가하면 인덱스도 같은 커밋에서 갱신한다.

---

## 4. 코드 규칙

### Rust

```bash
cargo fmt --all               # 커밋 전 필수
cargo clippy --all-targets -- -D warnings   # 경고 = 에러
cargo test                    # 로직 테스트는 네이티브에서 돈다
```

- 에디션 2021, MSRV는 `rust-toolchain.toml`에 고정
- 게임 로직 크레이트(`crates/sim`)는 **`wasm32` 의존성이 없어야 한다**. 네이티브에서 테스트 가능해야 함
- 부동소수점 결정성이 필요한 곳은 `f32` 대신 고정소수점(`i32` Q16.16) 사용 — `crates/sim/src/fixed.rs`
- 패닉 금지. `Result`로 올리고 최상단(`crates/app`)에서만 처리

### 크레이트 경계

```
crates/
├── sim/        # 순수 게임 로직. no_std 가능. 네트워크·렌더링 의존 없음
├── net/        # 프로토콜 직렬화 + 호스트/클라이언트 상태기계. sim에 의존
├── render/     # WebGL2 배칭 렌더러. sim의 상태를 읽기만 함
├── input/      # InputIntent 생성 (키보드/터치/게임패드)
└── app/        # wasm-bindgen 엔트리포인트. 위 넷을 조립
```

**의존 방향은 단방향이다.** `sim`은 아무것도 모른다. 역참조가 생기면 설계가 틀린 것이다.

### JavaScript (최소한만)

`web/` 아래 JS는 다음 세 가지만 담당한다. 그 외 로직이 JS로 새어나오면 리뷰에서 막는다.

1. Trystero 호출 래핑 → `wasm-bindgen` 콜백으로 전달
2. Canvas 엘리먼트 생성 + 리사이즈
3. 오디오 컨텍스트 언락 (모바일 자동재생 정책)

---

## 5. 커밋 · PR

- 커밋 메시지: `<scope>: <명령형 한 줄>` — 예) `net: 스냅샷 델타 인코딩 추가`
- scope는 크레이트명 또는 `docs`, `ci`, `assets`
- PR 본문에는 반드시:
  - 어떤 product-spec / design-doc을 구현했는지 링크
  - Hard Constraint 중 건드린 게 있으면 명시
  - 모바일에서 확인했으면 기기명 (없으면 "미확인"이라고 적는다. 거짓말하지 않는다)

---

## 6. 에이전트가 하면 안 되는 것

- **플레이스홀더 금지.** `// TODO: 나중에`, `const ASSET_URL = "example.com"`, 더미 데이터로 채운 화면 — 전부 금지. 못 만들겠으면 `docs/exec-plans/tech-debt-tracker.md`에 적고 넘어간다.
- **에셋을 상상해서 만들지 않는다.** `asset-manifest.md`에 없는 파일을 코드에서 참조하지 않는다.
- **벤치마크 숫자를 지어내지 않는다.** 측정 안 했으면 "미측정"이라고 쓴다.
- **라이브러리 API를 기억에 의존해 쓰지 않는다.** `docs/references/`에 요약이 있으면 그걸 보고, 없으면 만들고 나서 쓴다.
- **아키텍처를 임의로 바꾸지 않는다.** ARCHITECTURE.md와 어긋나는 구현이 필요하면 문서 수정 PR을 먼저 낸다.

---

## 7. 문서를 고칠 때의 파급

**수치나 결정을 바꾸면, 그걸 인용한 다른 문서를 전부 찾아 같이 고친다.**

실제로 한 번 놓쳤다: 맵 크기를 128×128 → 64×40으로 줄였는데 4개 문서가 옛 값을 유지했다 (tech-debt-tracker의 해결 기록 참조).

```bash
# 바꾸기 전에 어디서 인용하는지 확인
rg "128" docs/
rg "180초" docs/
```

문서끼리 어긋나면 **둘 중 하나가 버그다.** 어느 쪽인지 정하고 고친다. 방치하지 않는다 (CB-7).

---

## 8. 지금 어디까지 왔나

`docs/exec-plans/index.md` 를 보라. 진행 중인 계획과 각 계획이 **답해야 할 질문**이 거기 있다.

체크리스트를 다 채우는 게 목적이 아니다. 각 계획에는 산출물이 하나씩 있다 —
001은 연결 매트릭스, 002는 G1 실험 결과, 003은 성능 실측, 004는 플레이테스트 기록.

기술 부채는 `docs/exec-plans/tech-debt-tracker.md`.
